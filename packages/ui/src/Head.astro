---
interface Props {
  title: string;
  description?: string;
  ogImage?: string;
  canonical?: string;
  siteName?: string;
  themeColor?: string;
  noindex?: boolean;
  noCanonical?: boolean;
}

const {
  title,
  description = "Michal Ferber's web constellation: blog, services, and products.",
  ogImage,
  canonical,
  siteName = "Michal Ferber",
  themeColor,
  noindex = false,
  noCanonical = false,
} = Astro.props;

const canonicalURL = canonical || Astro.url.href;

// Social cards need an absolute URL; every site ships a 1200×630 /og.png default.
const ogImageURL = new URL(ogImage || "/og.png", Astro.site ?? Astro.url).href;

// Plausible (self-hosted, privacy-respecting — no Google Analytics, per CLAUDE.md).
// data-domain comes from each site's astro.config `site`, so the five domains report
// separately. Alias/mirror hosts (tgwab.us, michalferber.com/.net/.org/.us) serve the
// same build and therefore report under their canonical domain.
const plausibleDomain = Astro.site?.hostname;
---

<meta charset="UTF-8" />
<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
<title>{title}</title>
<meta name="description" content={description} />
{!noCanonical && <link rel="canonical" href={canonicalURL} />}
{noindex && <meta name="robots" content="noindex, follow" />}
<meta name="color-scheme" content="dark" />
{themeColor && <meta name="theme-color" content={themeColor} />}

<meta property="og:type" content="website" />
<meta property="og:site_name" content={siteName} />
<meta property="og:title" content={title} />
<meta property="og:description" content={description} />
{!noCanonical && <meta property="og:url" content={canonicalURL} />}
<meta property="og:image" content={ogImageURL} />
<meta property="og:image:width" content="1200" />
<meta property="og:image:height" content="630" />

<meta name="twitter:card" content="summary_large_image" />
<meta name="twitter:creator" content="@michalaferber" />
<meta name="twitter:title" content={title} />
<meta name="twitter:description" content={description} />
<meta name="twitter:image" content={ogImageURL} />

<script
  is:inline
  type="application/ld+json"
  set:html={JSON.stringify({
    "@context": "https://schema.org",
    "@type": "WebSite",
    name: siteName,
    url: (Astro.site ?? Astro.url).href,
  })}
/>

<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
<link rel="icon" href="/favicon.ico" sizes="32x32" />
<link rel="apple-touch-icon" href="/apple-touch-icon.png" />
<link rel="manifest" href="/site.webmanifest" />

{plausibleDomain && (
  <script
    is:inline
    defer
    data-domain={plausibleDomain}
    src="https://plausible.thompsonblack.us/js/script.outbound-links.file-downloads.tagged-events.js"
  ></script>
)}

<script is:inline type="text/javascript">
  (function (C, A, L) {
    let p = function (a, ar) {
      a.q.push(ar);
    };
    let d = C.document;
    C.Cal = C.Cal || function () {
      let cal = C.Cal;
      let ar = arguments;
      if (!cal.loaded) {
        cal.ns = {};
        cal.q = cal.q || [];
        d.head.appendChild(d.createElement("script")).src = A;
        cal.loaded = true;
      }
      if (ar[0] === L) {
        const api = function () {
          p(api, arguments);
        };
        const namespace = ar[1];
        api.q = api.q || [];
        if (typeof namespace === "string") {
          cal.ns[namespace] = cal.ns[namespace] || api;
          p(cal.ns[namespace], ar);
          p(cal, ["initNamespace", namespace]);
        } else {
          p(cal, ar);
        }
        return;
      }
      p(cal, ar);
    };
  })(window, "https://app.cal.com/embed/embed.js", "init");

  Cal("init", "meet", { origin: "https://app.cal.com" });
  Cal.ns.meet("ui", {
    cssVarsPerTheme: {
      light: { "cal-brand": "#292929" },
      dark: { "cal-brand": "#fafafa" },
    },
    hideEventTypeDetails: false,
    layout: "month_view",
  });

  const calConfig = JSON.stringify({
    layout: "month_view",
    useSlotsViewOnSmallScreen: true,
  });

  const applyCalConfig = () => {
    document.querySelectorAll('[data-cal-link="techguywithabeard/meet"]').forEach((node) => {
      node.setAttribute("data-cal-config", calConfig);
    });
  };

  if (document.readyState === "loading") {
    document.addEventListener("DOMContentLoaded", applyCalConfig, { once: true });
  } else {
    applyCalConfig();
  }
</script>
