node_modules
dist
.astro
.DS_Store
.env
.env.*
!.env.example

# Starter archive, not tracked
TGWAB-Project-Starter.zip

# docs/ is tracked (DS §10): it holds only what's already observable from the
# source or the public internet. Internal planning/strategy material MUST NOT
# be committed here — it lives in the private tgwab-planning repo (tgwab-web/).

# Claude Code local settings
.claude/settings.local.json

# secrets (§10/§16)
.dev.vars*
.env*
test-results/
playwright-report/
