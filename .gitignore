node_modules
dist
.astro
.DS_Store
.env
.env.*
!.env.example

# Starter archive, not tracked
TGWAB-Project-Starter.zip

# Internal planning/strategy docs — keep local, out of the public repo.
# domain-map.md is the exception: every fact in it is observable from the
# public internet, and README/CLAUDE point at it, so it ships with the repo.
docs/*
!docs/domain-map.md

# Claude Code local settings
.claude/settings.local.json

# secrets (§10/§16)
.dev.vars*
.env*
