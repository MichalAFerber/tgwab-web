# TGWAB Web Monorepo

Multi-site Astro monorepo for the TechGuyWithABeard brand + product portfolio.
Owner: Michal Ferber. Solo dev. 30+ years IT background — skip beginner explanations.

## What this repo is

The hub (`techguywithabeard.com`) plus per-product sites, all sharing a common design system.
Every property uses the same tokens, nav, and footer so the whole portfolio feels like one brand
even though each product ships on its own domain.

**Canonical properties:**
- `techguywithabeard.com` — hub: landing, blog, product portfolio
- `michalferber.dev` — wiki (outside this repo, keep on Jekyll)
- Product domains — each keeps its own domain, links back to hub

**Everything else is a 301 redirect.** See `@docs/domain-map.md`.

## Stack

- **Astro 5** (static). The `blog` Content Collection (typed via Zod) is the only
  collection; the portfolio is a plain TS data module, not a collection (see Conventions).
- **pnpm workspaces** — packages share via `workspace:*`
- **Vanilla CSS** with custom properties. No Tailwind, no CSS-in-JS.
- **Pagefind** for client-side search. `build:hub` indexes `dist/` after the Astro build;
  `/search` loads the index. No search index exists until you build.
- **Cloudflare Workers Static Assets** for deploy (`sites/hub/wrangler.jsonc`, `assets.directory`
  → `dist/`). *Not* Pages — the repo migrated off Pages. Cloudflare DNS/Registrar handles all domains.
- **Node 20** (see `.nvmrc`), **pnpm 9**

## Structure

```
tgwab-web/
├── CLAUDE.md                   # you're here
├── docs/                       # reference docs, load on demand
│   ├── scope.md                # full architecture + decisions
│   ├── phase-{1,2,3}-plan.md   # build plans (phase-3 superseded; see its header note)
│   ├── content-workflow.md     # how blog + portfolio content gets in (read before editing content)
│   └── domain-map.md           # canonical vs redirect domains
├── scripts/
│   └── migrate-blog.mjs        # pulls blog posts from Obsidian → src/content/blog
├── packages/
│   ├── design-tokens/          # CSS variables, base styles, fonts (tokens/base/fonts.css)
│   └── ui/                     # shared Astro components (Layout, Nav, Footer, Head, PdfViewer)
└── sites/
    └── hub/                    # techguywithabeard.com — the only site so far
        └── src/
            ├── content/blog/   # GENERATED by migrate-blog.mjs — don't hand-edit
            └── data/portfolio.ts  # portfolio entries (plain TS module)
```

Per-product sites (`resizewizard.app`, `mykk.us`, …) are planned but not yet in `sites/`.

## Conventions

- **Package names:** `@tgwab/<name>`. Workspace deps use `"workspace:*"`.
- **CSS:** All colors/spacing/type come from `@tgwab/design-tokens/tokens.css`.
  Never hardcode a hex or px value outside that file. If you need a new token, add it there.
- **Components:** Shared UI lives in `packages/ui/src/*.astro` and is imported as
  `@tgwab/ui/<Component>.astro`. Site-specific components live in `sites/<site>/src/components/`.
- **Layout prop contract:** `<Layout>` accepts `title`, `description`, `ogImage`, `productName`,
  `showNav` (default `true`). Product sites pass `productName` so the footer shows the
  "A TGWAB product" badge.
- **Blog content:** Files in `sites/hub/src/content/blog/` are **generated** from Obsidian by
  `pnpm migrate` — edits there get overwritten. Author in Obsidian; the source of truth is
  `~/Obsidian/Obsidian-Master/blog/posts/`. Schema lives in `sites/hub/src/content/config.ts`
  (never bypass it). The migrator emits `.md`, or `.mdx` when a post embeds a PDF/video.
  See `@docs/content-workflow.md`.
- **Portfolio:** Edit `sites/hub/src/data/portfolio.ts` directly — it's a typed TS array, not a
  collection and has no migration step. Both `/portfolio/` and the landing page's "Featured work"
  (`featured: true`) read from it.
- **Links between properties:** Use absolute URLs (`https://techguywithabeard.com/portfolio/`)
  since each site is deployed standalone. Don't assume same-origin routing.
- **Dates in front matter:** ISO 8601 (`2026-04-21`), no time component unless needed.

## Voice (when generating blog content)

TechGuyWithABeard: casual, opinionated, anti-AI-sounding. No "In today's fast-paced world,"
no corporate hedging, no "It's important to note that." Contractions are fine. Strong takes
are the point. Technical claims should be specific enough to defend.

## Commands

```bash
# Install everything (run from repo root)
pnpm install

# Dev server for hub
pnpm dev:hub
# → http://localhost:4321

# Build hub for production (Astro build + Pagefind index)
pnpm build:hub
pnpm preview:hub     # serve dist/ locally; needed to test /search (Pagefind)

# Build every site
pnpm build

# Pull blog posts from Obsidian into src/content/blog (run before committing content)
pnpm migrate
```

There is no test suite or linter configured; "verification" means a clean `pnpm build:hub`.

**Remote testing:** the headless box runs Caddy serving each site's built `dist/` at
`<site>.dev.mykk.foo` (hub → `https://hub.dev.mykk.foo`), browsable over the LAN. Rebuild
with `pnpm build:hub` and refresh — no Caddy restart needed. Setup + runbook: `@docs/dev-server.md`.

## What to work on

Build plans live in `docs/`: `phase-1-plan.md` (hub) and `phase-2-plan.md` (content migration)
carry `[ ]` checkboxes — read them at session start to see what's done, check off as you go.
`phase-3-plan.md` is **superseded** (wiki removed, portfolio re-homed) — kept only as history.

## Hard rules

- Don't add runtime JS frameworks (React, Vue, Solid) without a concrete reason.
  Astro islands only, and only when a feature genuinely needs interactivity.
- Don't pull in Google Fonts, Google Analytics, or any Google-hosted assets.
  Self-host or use privacy-respecting alternatives (Bunny Fonts, Plausible, Cloudflare Web Analytics).
- Don't commit `.env` files. If env vars are needed, document them in `.env.example`.
- Don't touch `michalferber.dev` — it's a separate Jekyll repo.
- Before adding a dependency, ask: does this belong in a shared package or a single site?
  Shared code goes in `packages/`, site-specific in `sites/<site>/`.
